<% if( game && game.length > 0) { %>
	<table>
		<tbody>
		<% for(var i=0; i < game.length; i++) { %>
		<tr>
		    <td>Group<%= i+1 %>&nbsp;</td>
			    <% for (var j = 0; j < game[i].user_ids.length; j ++) { %>
			    	<td><%= game[i].user_ids[j].name %></td>
			    <% } %>
		    <td><%= game[i].teetime %></td>
		</tr>
		<% } %>
		</tbody>
	</table>
<% } %>